<div class="container">
  <h1>New List</h1>
  <%= simple_form_for(@list) do |f|  %>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>
  <%= link_to "Back to Lists", lists_path, class: "btn btn-secondary mt-3" %>
</div>
